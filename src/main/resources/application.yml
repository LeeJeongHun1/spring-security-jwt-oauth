server:
  port: 8088

#security:
#  basic:
#    enabled: false # security ?? ?? ?? ??
#logging:
#  level:
#    org.hibernate.type: trace # JPA? ???? ??? ???? ? ??

spring:
  devtools:
    livereload:
      enabled: true
  jpa: #jpa
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.format_sql: true
  h2:
    console:
      path: /h2-console
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver  # Database? H2? ?????.
    url: jdbc:h2:~/test  # H2 ?? ??
    username: sa  # H2 ?? ? ??? username ?? (??? ??? ??)
    password:  # H2 ?? ? ??? pa
